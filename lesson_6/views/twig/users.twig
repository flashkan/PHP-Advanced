{% for user in users %}
    <a href="/user/user?id={{ user.id }}">
        <p>{{ user.userName }}</p>
        <p>{{ user.userLogin }}</p>
        <p>{{ user.userEmail }}</p>
    </a>
    <hr>
{% endfor %}